#!/usr/bin/env bash

url=https://github.com/heroku/heroku-buildpack-ruby
dir=$(mktemp -t buildpackXXXXX)
rm -rf $dir

echo "=====> Cloning: $url"
git clone $url $dir >/dev/null 2>&1

cd $dir
ruby -I$dir/lib $1/compile.rb $1 $2 $3
$dir/bin/release $1 > $1/release.out
